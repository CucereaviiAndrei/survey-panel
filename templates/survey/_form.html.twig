{{ form_start(form) }}
<div class="row g-3">
    {{ form_row(form.name) }}
    {{ form_row(form.status) }}
</div>

<button class="btn btn-success mt-3">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
