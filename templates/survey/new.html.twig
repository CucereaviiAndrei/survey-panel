{% extends 'base.html.twig' %}

{% block title %}New Survey{% endblock %}

{% block body %}
    <h1>Create Survey</h1>

    {{ include('survey/_form.html.twig', { form: form, button_label: 'Create' }) }}

    <a href="{{ path('survey_index') }}" class="btn btn-link mt-3">Back to list</a>
{% endblock %}
